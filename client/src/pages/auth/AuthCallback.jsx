export default function AuthCallback() {
  // TODO: extract token from URL, store in localStorage, redirect to /
  return (
    <div className="flex min-h-screen items-center justify-center bg-slate-100 text-slate-700">
      Processing login...
    </div>
  )
}
